@page
@model MiniCinema.Pages.MovieSessions.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.MovieSession[0].ShowingDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.MovieSession[0].Hall)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.MovieSession[0].MovieDetail)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.MovieSession) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ShowingDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Hall.HallId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.MovieDetail.Genre)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.MovieSessionId">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.MovieSessionId">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.MovieSessionId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
